#include <iosteam>
#include <Padawan.h>

using namesapce std;

Padawan::Padawan(string init_name) // Fill out the rest

string Padawan::getName() // Fill out the rest

int Padawan::getMidi() // Fill out the rest

void Padawan::setMidi() // Fill out the rest

void Padawan::checkJedi() // Fill out the rest

int main() {
	//Make three Padawans, scan in three midichlorian counts, check if each is a Jedi or not

	return 0;
}
